name: Summary CI
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  summary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: upptime/summary-generator@v1.0.2
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
